server:
  port: 8084

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/enrollmentdb
    username: postgres
    password: reddy
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
